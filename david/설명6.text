DDOS 공격차단, 사용자의 API사용량 제한 등의 기능 추가하기.
우선 사용자가 API 요청을 보낼때마다 사용횟수가 증가하고, 한도에 도달한 경우 사용량을 초과했다는 응답을 반환하도록 해야한다.

I want to you to implement rate limiting protection to /github-summarizer api in @route.js
I want each time a user invokes a certain api key, to increment the api key key usage column of the corresponding api key in supabase.
I want you then to check if the usage is lower than the limit of api key.
If it is larger you should return in the response a 429 and that there is a rate limit

이제 이 코드들을 재사용하고 엔드포인트에서 쓸 수 있게끔 해주는 구현을 요청한다. 이건 무슨뜻인지 잘 모르겠다.
this is good but make the code more reusable so create a function that checks the limit so it can be used in future endpoints.

